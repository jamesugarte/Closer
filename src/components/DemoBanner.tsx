export function DemoBanner() {
  return (
    <p className="text-center text-[10px] font-medium tracking-wide text-[var(--muted)]/80">
      Demo environment — simulated financial data
    </p>
  );
}
